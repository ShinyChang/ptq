{
  "name": "ptq",
  "version": "0.1.7",
  "description": "promise task queue",
  "main": "dist/index.js",
  "repository": "https://github.com/ShinyChang/ptq.git",
  "author": "Shiny <contact@shinychang.net>",
  "license": "MIT",
  "scripts": {
    "build": "babel index.js -o dist/index.js",
    "clean": "rm -rf dist && mkdir dist",
    "release": "yarn run clean && yarn run build && yarn publish",
    "start": "node index.js",
    "test": "jest",
    "watch": "jest --watch"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^21.2.1"
  }
}
